@model IEnumerable<Fttx_Orm.Models.Process.IsTur>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IsTuru)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IsTuru)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IsTurId }) |
            @Html.ActionLink("Details", "Details", new { id=item.IsTurId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IsTurId })
        </td>
    </tr>
}

</table>
