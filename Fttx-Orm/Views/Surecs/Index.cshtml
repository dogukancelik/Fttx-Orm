@model IEnumerable<Fttx_Orm.Models.Process.Surec>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SurecAdi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SurecAdi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SurecId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SurecId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SurecId })
        </td>
    </tr>
}

</table>
