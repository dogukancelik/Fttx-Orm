@model IEnumerable<Fttx_Orm.Models.Process.RoleSurec>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoleId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SurecId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoleId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SurecId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoleSurecId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoleSurecId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoleSurecId })
        </td>
    </tr>
}

</table>
